apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    metadata:
      annotations:
        iam.amazonaws.com/role: 
    spec:
      containers:
      # Change the value of image field below to your controller image URL
      - image: 213074200201.dkr.ecr.eu-west-2.amazonaws.com/apigw:latest
        name: manager
